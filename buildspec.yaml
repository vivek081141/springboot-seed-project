version: 0.2
phases:
  install:
    commands:
      - echo "install"
  pre_build:
    commands:
      - echo "pre_build"
  build:
    commands:
    - echo "build"
    - mvn package -D spring.profiles.active=dev
  post_build:
    commands:
    - echo "post_build"